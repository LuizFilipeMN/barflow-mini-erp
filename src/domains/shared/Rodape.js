import React from 'react'
import { View, StyleSheet } from 'react-native'

const Rodape = () => {
  return <View style={styles.rodape} />
}

const styles = StyleSheet.create({
  rodape: {
    height: 40,
    backgroundColor: '#d1cdbc'
  }
})

export default Rodape
